// use std::cell::Cell;
// use cpu::*;


#[test]
fn immediate() {
    //    let cycles = Cell::new(0);
    //    let mut cpu = TestCpu::with_memory(&[0x0]);
    //    cpu.step()
    //    assert_eq!(2, cycles);
}
