language: rust
rust:
  - nightly

before_install:
  - sudo add-apt-repository ppa:ubuntu -y
  - sudo apt-get update
  - sudo apt-get install -y sndio

script:
- cargo build --verbose --examples --tests --all-features
- cargo test --verbose --all-features
